import { theme } from 'assets/styles/theme';

export const styles = {
  table: { mt: 2, minWidth: theme.spacing(50) },
  header: { fontWeight: 500 },
  amount: { fontWeight: 400 },
  total: {
    paddingLeft: 2,
    fontWeight: 500,
    borderBottom: 'none',
  },
};
